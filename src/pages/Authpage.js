import React from 'react';
import AuthForm from '../Components/Auth/AuthForm';

const Authpage = () => {
    return (
        <>
            <AuthForm />
        </>
    );
};
export default Authpage;
